apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: inventory-system

data:
  PORT: "5000"

  DB_HOST: mysql-service
  DB_PORT: "3306"
  DB_NAME: inventory_db

  NODE_ENV: production